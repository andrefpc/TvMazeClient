package com.andrefpc.tvmazeclient.util

import com.andrefpc.tvmazeclient.core.data.SearchPeople

object SearchPeopleMocks {
    val searchPeople = SearchPeople(
        person = PersonMocks.person
    )
}